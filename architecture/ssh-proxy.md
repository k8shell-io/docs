# SSH Proxy 

```{abstract}
SSH Proxy provides the secure shell protocol interface, enabling clients to connect using standard SSH-based tools. 
```

It translates the ssh protocol into K8shell streaming API calls using GRPC provided by the [k8shelld](k8shelld.md). 

