# k8shell proxy 

K8shell-proxy is the core component of k8shell services. It consists of a set of components that facilitate workspace provisioning and workspace access for users. The proxy acts as a gateway between the client and the Kubernetes cluster, enabling users to interact with the cluster through standard SSH protocol. This approach allows users to access the cluster without requiring additional tools or plugins on their local machines. 
